init {
    a, b, c : Int
    x, y, z : Float
    nombre, apellido, resultadoTexto : String
}

a := 10
b := 5
c := 0
x := 99.5
y := 3.14159
nombre := "Juan"
apellido := "Perez"


c =: a + b
x =: x * 2.0
a =: (a + b) * c


write("Programa de prueba integral")
write("Ingrese un valor para la variable c:")
read(c)
write("Valor ingresado:")
write(c)


if (a > b)
{
    write("a es mayor que b")
    if (c > 0 AND c < 100)
    {
        write("c está en rango válido")
        c =: c + 10
    }
    else
    {
        write("c fuera de rango")
        c := 50
    }
}


while (b < a)
{
    write("Incrementando b")
    b =: b + 2
    a =: a - 1
}


resultadoTexto = sliceAndConcat(1, 3, "Hola", "Mundo", 1)
write("Resultado de sliceAndConcat:")
write(resultadoTexto)


write("Demostrando reorder:")
reorder([a+5, b*2, c-10, x+y], 1, 0)


if (a >= b OR c >= 20)
{
    write("Condición evaluada como verdadera")
    z =: y / 2.0
    
    while (z > 1.0)
    {
        z =: z - 0.5
        write(z)
    }
}


if (NOT a < b)
{
    write("a no es menor que b")
}

write("Fin del programa") 